{{#each data}}
    <ul>
        <li>{{this.title}}</li>
        <li><a href="{{this.link}}">{{this.link}}</a></li>
        <button data-id="{{this._id}}">Remove from Favorites</button>
    </ul>
{{/each}}